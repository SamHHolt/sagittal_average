[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "Sagittal_Average"
version = "0.1.0"

[tool.hatch.build.targets.wheel]
packages = [
  "src/sag_brain_avg",
]